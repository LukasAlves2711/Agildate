import { FindServiceFromKeyPipe } from './find-service-from-key.pipe';

describe('FindServiceFromKeyPipe', () => {
  it('create an instance', () => {
    const pipe = new FindServiceFromKeyPipe();
    expect(pipe).toBeTruthy();
  });
});
